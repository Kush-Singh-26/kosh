# GO SSG

- This is a static site generator (ssg) in golang.
- To build the site :

```bash
go run builder/main.go
```

- To deploy locally

```bash
go run server/main.go
```

- To create a new file

```bash
go run new.go "<title>"
```