//go:build !wasm

// Package asset provides static asset discovery, copying, and processing for Kosh builds.
// It handles image optimization, WebP conversion, and esbuild integration for hashed assets.
package asset
